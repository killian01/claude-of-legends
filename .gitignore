node_modules/
dist/
dist-server/
# Runtime server state (players, match log); never version player data.
data/
.turbo/
.env
.env.*
!.env.example
*.local
coverage/
# Local research notes that cite competitor games by name (kit redesign catalogue).
# Useful locally, never committed; see the header of docs/design/kits-v2.md.
docs/research/
# Raw 2048px icon sources as generated, kept locally for regeneration.
# 200 MB and counting; what ships is the 512px conversion in public/icons/.
art_src/
