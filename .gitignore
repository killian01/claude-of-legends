node_modules/
dist/
dist-server/
dist-env/
# Runtime server state (players, match log); never version player data.
data/
.turbo/
.env
.env.*
!.env.example
*.local
coverage/
# Screencast frames from scripts/shot_replay.mjs; what ships is the gif
# assembled from them, not the thousand stills.
docs/screenshots/frame-*.jpg
# Local research notes that cite competitor games by name (kit redesign catalogue).
# Useful locally, never committed; see the header of docs/design/kits-v2.md.
docs/research/
# Raw art as generated, kept locally for regeneration: 2048px icon sources,
# the untouched Meshy character exports and the intro master. 200 MB and
# counting; what ships is the conversion under public/ (scripts/convert_art.mjs
# for the paintings, scripts/shrink_models.mjs for the GLBs).
art_src/
.pnpm-store/
dist-gate*/
dist-matrix*/
