node_modules/
dist/
dist-server/
# Runtime server state (players, match log); never version player data.
data/
.turbo/
.env
.env.*
!.env.example
*.local
coverage/
# Local research notes that cite competitor games by name (kit redesign catalogue).
# Useful locally, never committed; see the header of docs/design/kits-v2.md.
docs/research/
