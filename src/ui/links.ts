// Where the project lives outside the game. Three links, in one place,
// because they are spoken by screens that share nothing else: the landing
// bar, the account drawer, and the card at the end of a match.
export const REPO = 'https://github.com/killian01/claude-of-legends';
export const DISCORD = 'https://discord.gg/uURYY5qYJE';
// The privacy page lives in the repository rather than on the site, which
// is the point: there it sits beside the code that has to make it true,
// and a reader can check it instead of believing it.
export const PRIVACY = `${REPO}/blob/main/PRIVACY.md`;
