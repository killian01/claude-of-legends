node_modules
dist
dist-server
.git
.claude
docs/screenshots
# Raw art sources: 200 MB that no build step reads.
art_src
*.log
.env
.env.*

# Runtime state and local scratch: 69 MB the build never reads, and
# player data that must never be baked into an image.
data
